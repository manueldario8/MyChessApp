<Window x:Class="MyChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyChessUI"
        mc:Ignorable="d"
        Title="Chess" SizeToContent="Height" Width="630"
        Icon="/Assets/icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#101010"
        KeyDown="Window_KeyDown">
    <Viewbox>
        <Grid x:Name="BoardGrid" Width="630" Height="630"
              MouseDown="BoardGrid_MouseDown">

            <!-- Fondo del tablero -->
            <Grid.Background>
                <ImageBrush ImageSource="/Assets/Board.png"/>
            </Grid.Background>

            <!-- Resaltado de casillas -->
            <UniformGrid x:Name="HighLightGrid" Rows="8" Columns="8"/>

            <!-- Piezas -->
            <UniformGrid x:Name="PieceGrid" Rows="8" Columns="8" RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Contenedor para el menú / tarjeta -->
            <ContentControl x:Name="MenuContainer"/>
        </Grid>
    </Viewbox>
</Window>
